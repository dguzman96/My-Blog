<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, "Heading" %>
    <%= form.text_field :title, placeholder: "Have a creative title?" %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, size: "10x3" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
